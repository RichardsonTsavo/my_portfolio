import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';
import 'package:flutter_modular/flutter_modular_test.dart';

import 'package:my_portfolio/app/modules/calculations/calculations_page.dart';

void main() {
  testWidgets('CalculationsPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(CalculationsPage(title: 'Calculations')));
    //  final titleFinder = find.text('Calculations');
    //  expect(titleFinder, findsOneWidget);
  });
}
